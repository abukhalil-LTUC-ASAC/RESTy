import React, { Component } from 'react';
import './header.scss'

class Header extends Component {
  render() {
    return <header>
        <div>
          RESTy
        </div>
    </header>
  }
}

export default Header;
